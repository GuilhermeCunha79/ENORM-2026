plugins {
    id "io.freefair.plantuml" version "9.2.0"
}

repositories {
    mavenCentral()
}

apply plugin:'base'

plantUml {
    source("diagrams")
    includePattern = "**/*.puml"
    fileFormat = "PNG"
    outputDirectory = layout.projectDirectory.dir("diagramsOut")
}