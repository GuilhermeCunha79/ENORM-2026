package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.OrderService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.Order;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedOrderController {
  private final OrderService resourceService;
  protected GeneratedOrderController(OrderService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "orders")
  public List<Order> listOrder() {
    return resourceService.listOrder();
  }
  @GetMapping(value = "orders/{id}")
  public Order getOrder(@PathVariable Long id) {
    return resourceService.getOrder(id);
  }
  @PostMapping(value = "orders")
  public ResponseEntity<Order> createOrder(@Valid @RequestBody Order request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createOrder(request));
  }

  @DeleteMapping(value = "orders/{id}")
  public ResponseEntity<Void> deleteOrder(@PathVariable Long id) {
    resourceService.deleteOrder(id);
    return ResponseEntity.noContent().build();
  }
}
