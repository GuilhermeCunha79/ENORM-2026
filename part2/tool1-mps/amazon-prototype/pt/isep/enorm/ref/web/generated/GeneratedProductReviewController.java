package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.ProductReviewService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.ProductReview;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedProductReviewController {
  private final ProductReviewService resourceService;
  protected GeneratedProductReviewController(ProductReviewService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "productreviews")
  public List<ProductReview> listProductReview() {
    return resourceService.listProductReview();
  }
  @GetMapping(value = "productreviews/{id}")
  public ProductReview getProductReview(@PathVariable Long id) {
    return resourceService.getProductReview(id);
  }
  @PostMapping(value = "productreviews")
  public ResponseEntity<ProductReview> createProductReview(@Valid @RequestBody ProductReview request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createProductReview(request));
  }

  @DeleteMapping(value = "productreviews/{id}")
  public ResponseEntity<Void> deleteProductReview(@PathVariable Long id) {
    resourceService.deleteProductReview(id);
    return ResponseEntity.noContent().build();
  }
}
