package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.CommentReviewService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.CommentReview;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedCommentReviewController {
  private final CommentReviewService resourceService;
  protected GeneratedCommentReviewController(CommentReviewService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "commentreviews")
  public List<CommentReview> listCommentReview() {
    return resourceService.listCommentReview();
  }
  @GetMapping(value = "commentreviews/{id}")
  public CommentReview getCommentReview(@PathVariable Long id) {
    return resourceService.getCommentReview(id);
  }
  @PostMapping(value = "commentreviews")
  public ResponseEntity<CommentReview> createCommentReview(@Valid @RequestBody CommentReview request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createCommentReview(request));
  }

  @DeleteMapping(value = "commentreviews/{id}")
  public ResponseEntity<Void> deleteCommentReview(@PathVariable Long id) {
    resourceService.deleteCommentReview(id);
    return ResponseEntity.noContent().build();
  }
}
