package pt.isep.enorm.ref.service.generated;

/*Generated by MPS */

import org.springframework.transaction.annotation.Transactional;
import pt.isep.enorm.ref.repository.ProductRepository;
import java.util.List;
import pt.isep.enorm.ref.domain.Product;
import pt.isep.enorm.ref.web.error.ResourceNotFoundException;

@Transactional(readOnly = true)
public abstract class GeneratedProductService {
  private final ProductRepository ProductRepository;
  protected GeneratedProductService(ProductRepository ProductRepository) {
    this.ProductRepository = ProductRepository;
  }
  public List<Product> listProduct() {
    return ProductRepository.findAll();
  }
  public Product getProduct(final Long id) {
    return ProductRepository.findById(id).orElseThrow(() -> new ResourceNotFoundException("Product '%s' was not found.".formatted(id)));
  }
  @Transactional
  public Product createProduct(Product request) {
    if (request == null) {
      throw new IllegalArgumentException("Product payload is required.");
    }
    return ProductRepository.save(request);
  }
  @Transactional
  public void deleteProduct(Long id) {
    ProductRepository.delete(getProduct(id));
  }

  protected void afterProductCreated(Product savedResource) {
  }
}
