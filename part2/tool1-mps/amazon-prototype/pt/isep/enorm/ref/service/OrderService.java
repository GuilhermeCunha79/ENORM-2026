package pt.isep.enorm.ref.service;

/*Generated by MPS */

import org.springframework.stereotype.Service;
import pt.isep.enorm.ref.service.generated.GeneratedOrderService;
import pt.isep.enorm.ref.repository.OrderRepository;
import pt.isep.enorm.ref.domain.Order;
import pt.isep.enorm.ref.domain.enums.TriggerEvent;

@Service
public class OrderService extends GeneratedOrderService {

  private final ModerationService moderationService;

  public OrderService(OrderRepository feedbackRepository, ModerationService moderationServices) {
    super(feedbackRepository);
    this.moderationService = moderationServices;
  }

  @Override
  protected void afterOrderCreated(Order savedResource) {
    moderationService.moderateAutomatically("Resource", savedResource.getId(), TriggerEvent.ON_RESOURCE_CREATE, moderationContent(savedResource));
  }
  protected String moderationContent(Order savedResource) {
    return savedResource.toString();
  }

}
