package pt.isep.enorm.ref.service;

/*Generated by MPS */

import org.springframework.stereotype.Service;
import pt.isep.enorm.ref.service.generated.GeneratedProductService;
import pt.isep.enorm.ref.repository.ProductRepository;
import pt.isep.enorm.ref.domain.Product;
import pt.isep.enorm.ref.domain.enums.TriggerEvent;

@Service
public class ProductService extends GeneratedProductService {

  private final ModerationService moderationService;

  public ProductService(ProductRepository feedbackRepository, ModerationService moderationServices) {
    super(feedbackRepository);
    this.moderationService = moderationServices;
  }

  @Override
  protected void afterProductCreated(Product savedResource) {
    moderationService.moderateAutomatically("Resource", savedResource.getId(), TriggerEvent.ON_RESOURCE_CREATE, moderationContent(savedResource));
  }
  protected String moderationContent(Product savedResource) {
    return savedResource.toString();
  }

}
