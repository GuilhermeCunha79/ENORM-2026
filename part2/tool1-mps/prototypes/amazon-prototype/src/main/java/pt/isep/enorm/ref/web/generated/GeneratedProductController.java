package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.ProductService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.Product;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedProductController {
  private final ProductService resourceService;
  protected GeneratedProductController(ProductService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "products")
  public List<Product> listProduct() {
    return resourceService.listProduct();
  }
  @GetMapping(value = "products/{id}")
  public Product getProduct(@PathVariable Long id) {
    return resourceService.getProduct(id);
  }
  @PostMapping(value = "products")
  public ResponseEntity<Product> createProduct(@Valid @RequestBody Product request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createProduct(request));
  }

  @DeleteMapping(value = "products/{id}")
  public ResponseEntity<Void> deleteProduct(@PathVariable Long id) {
    resourceService.deleteProduct(id);
    return ResponseEntity.noContent().build();
  }
}
