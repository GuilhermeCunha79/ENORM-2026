package pt.isep.enorm.ref.service.generated;

/*Generated by MPS */

import org.springframework.transaction.annotation.Transactional;
import pt.isep.enorm.ref.repository.OrderRepository;
import java.util.List;
import pt.isep.enorm.ref.domain.Order;
import pt.isep.enorm.ref.web.error.ResourceNotFoundException;

@Transactional(readOnly = true)
public abstract class GeneratedOrderService {
  private final OrderRepository OrderRepository;
  protected GeneratedOrderService(OrderRepository OrderRepository) {
    this.OrderRepository = OrderRepository;
  }
  public List<Order> listOrder() {
    return OrderRepository.findAll();
  }
  public Order getOrder(final Long id) {
    return OrderRepository.findById(id).orElseThrow(() -> new ResourceNotFoundException("Order '%s' was not found.".formatted(id)));
  }
  @Transactional
  public Order createOrder(Order request) {
    if (request == null) {
      throw new IllegalArgumentException("Order payload is required.");
    }
    return OrderRepository.save(request);
  }
  @Transactional
  public void deleteOrder(Long id) {
    OrderRepository.delete(getOrder(id));
  }

  protected void afterOrderCreated(Order savedResource) {
  }
}
