package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.VideoService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.Video;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedVideoController {
  private final VideoService resourceService;
  protected GeneratedVideoController(VideoService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "videos")
  public List<Video> listVideo() {
    return resourceService.listVideo();
  }
  @GetMapping(value = "videos/{id}")
  public Video getVideo(@PathVariable Long id) {
    return resourceService.getVideo(id);
  }
  @PostMapping(value = "videos")
  public ResponseEntity<Video> createVideo(@Valid @RequestBody Video request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createVideo(request));
  }

  @DeleteMapping(value = "videos/{id}")
  public ResponseEntity<Void> deleteVideo(@PathVariable Long id) {
    resourceService.deleteVideo(id);
    return ResponseEntity.noContent().build();
  }
}
