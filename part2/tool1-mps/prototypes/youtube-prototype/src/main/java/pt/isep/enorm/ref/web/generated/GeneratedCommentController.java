package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.CommentService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.Comment;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedCommentController {
  private final CommentService resourceService;
  protected GeneratedCommentController(CommentService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "comments")
  public List<Comment> listComment() {
    return resourceService.listComment();
  }
  @GetMapping(value = "comments/{id}")
  public Comment getComment(@PathVariable Long id) {
    return resourceService.getComment(id);
  }
  @PostMapping(value = "comments")
  public ResponseEntity<Comment> createComment(@Valid @RequestBody Comment request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createComment(request));
  }

  @DeleteMapping(value = "comments/{id}")
  public ResponseEntity<Void> deleteComment(@PathVariable Long id) {
    resourceService.deleteComment(id);
    return ResponseEntity.noContent().build();
  }
}
