package pt.isep.enorm.ref.service;

/*Generated by MPS */

import org.springframework.stereotype.Service;
import pt.isep.enorm.ref.service.generated.GeneratedCommentService;
import pt.isep.enorm.ref.repository.CommentRepository;
import pt.isep.enorm.ref.domain.Comment;
import pt.isep.enorm.ref.domain.enums.TriggerEvent;

@Service
public class CommentService extends GeneratedCommentService {

  private final ModerationService moderationService;

  public CommentService(CommentRepository feedbackRepository, ModerationService moderationServices) {
    super(feedbackRepository);
    this.moderationService = moderationServices;
  }

  @Override
  protected void afterCommentCreated(Comment savedResource) {
    moderationService.moderateAutomatically("Comment", savedResource.getId(), TriggerEvent.ON_RESOURCE_CREATE, moderationCommentContent(savedResource));
  }


  protected String moderationCommentContent(Comment savedResource) {
    return savedResource.toString();
  }

}
