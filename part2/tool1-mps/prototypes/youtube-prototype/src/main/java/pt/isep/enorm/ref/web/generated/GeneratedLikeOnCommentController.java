package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.LikeOnCommentService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.LikeOnComment;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedLikeOnCommentController {
  private final LikeOnCommentService resourceService;
  protected GeneratedLikeOnCommentController(LikeOnCommentService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "likeoncomments")
  public List<LikeOnComment> listLikeOnComment() {
    return resourceService.listLikeOnComment();
  }
  @GetMapping(value = "likeoncomments/{id}")
  public LikeOnComment getLikeOnComment(@PathVariable Long id) {
    return resourceService.getLikeOnComment(id);
  }
  @PostMapping(value = "likeoncomments")
  public ResponseEntity<LikeOnComment> createLikeOnComment(@Valid @RequestBody LikeOnComment request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createLikeOnComment(request));
  }

  @DeleteMapping(value = "likeoncomments/{id}")
  public ResponseEntity<Void> deleteLikeOnComment(@PathVariable Long id) {
    resourceService.deleteLikeOnComment(id);
    return ResponseEntity.noContent().build();
  }
}
