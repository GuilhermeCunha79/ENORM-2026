package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.ChannelService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.Channel;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedChannelController {
  private final ChannelService resourceService;
  protected GeneratedChannelController(ChannelService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "channels")
  public List<Channel> listChannel() {
    return resourceService.listChannel();
  }
  @GetMapping(value = "channels/{id}")
  public Channel getChannel(@PathVariable Long id) {
    return resourceService.getChannel(id);
  }
  @PostMapping(value = "channels")
  public ResponseEntity<Channel> createChannel(@Valid @RequestBody Channel request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createChannel(request));
  }

  @DeleteMapping(value = "channels/{id}")
  public ResponseEntity<Void> deleteChannel(@PathVariable Long id) {
    resourceService.deleteChannel(id);
    return ResponseEntity.noContent().build();
  }
}
