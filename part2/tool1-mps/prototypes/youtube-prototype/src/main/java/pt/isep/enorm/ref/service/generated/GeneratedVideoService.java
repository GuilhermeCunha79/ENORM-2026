package pt.isep.enorm.ref.service.generated;

/*Generated by MPS */

import org.springframework.transaction.annotation.Transactional;
import pt.isep.enorm.ref.repository.VideoRepository;
import java.util.List;
import pt.isep.enorm.ref.domain.Video;
import pt.isep.enorm.ref.web.error.ResourceNotFoundException;

@Transactional(readOnly = true)
public abstract class GeneratedVideoService {
  private final VideoRepository VideoRepository;
  protected GeneratedVideoService(VideoRepository VideoRepository) {
    this.VideoRepository = VideoRepository;
  }
  public List<Video> listVideo() {
    return VideoRepository.findAll();
  }
  public Video getVideo(final Long id) {
    return VideoRepository.findById(id).orElseThrow(() -> new ResourceNotFoundException("Video '%s' was not found.".formatted(id)));
  }
  @Transactional
  public Video createVideo(Video request) {
    if (request == null) {
      throw new IllegalArgumentException("Video payload is required.");
    }
    return VideoRepository.save(request);
  }
  @Transactional
  public void deleteVideo(Long id) {
    VideoRepository.delete(getVideo(id));
  }

  protected void afterVideoCreated(Video savedResource) {
  }
}
