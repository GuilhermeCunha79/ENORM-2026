package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.LikeOnVideoService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.LikeOnVideo;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedLikeOnVideoController {
  private final LikeOnVideoService resourceService;
  protected GeneratedLikeOnVideoController(LikeOnVideoService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "likeonvideos")
  public List<LikeOnVideo> listLikeOnVideo() {
    return resourceService.listLikeOnVideo();
  }
  @GetMapping(value = "likeonvideos/{id}")
  public LikeOnVideo getLikeOnVideo(@PathVariable Long id) {
    return resourceService.getLikeOnVideo(id);
  }
  @PostMapping(value = "likeonvideos")
  public ResponseEntity<LikeOnVideo> createLikeOnVideo(@Valid @RequestBody LikeOnVideo request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createLikeOnVideo(request));
  }

  @DeleteMapping(value = "likeonvideos/{id}")
  public ResponseEntity<Void> deleteLikeOnVideo(@PathVariable Long id) {
    resourceService.deleteLikeOnVideo(id);
    return ResponseEntity.noContent().build();
  }
}
