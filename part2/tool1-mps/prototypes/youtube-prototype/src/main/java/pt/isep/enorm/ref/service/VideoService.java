package pt.isep.enorm.ref.service;

/*Generated by MPS */

import org.springframework.stereotype.Service;
import pt.isep.enorm.ref.service.generated.GeneratedVideoService;
import pt.isep.enorm.ref.repository.VideoRepository;
import pt.isep.enorm.ref.domain.Video;
import pt.isep.enorm.ref.domain.enums.TriggerEvent;

@Service
public class VideoService extends GeneratedVideoService {

  private final ModerationService moderationService;

  public VideoService(VideoRepository feedbackRepository, ModerationService moderationServices) {
    super(feedbackRepository);
    this.moderationService = moderationServices;
  }

  @Override
  protected void afterVideoCreated(Video savedResource) {
    moderationService.moderateAutomatically("Video", savedResource.getId(), TriggerEvent.ON_RESOURCE_CREATE, moderationVideoContent(savedResource));
  }


  protected String moderationVideoContent(Video savedResource) {
    return savedResource.toString();
  }

}
