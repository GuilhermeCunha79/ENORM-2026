package pt.isep.enorm.ref.web.generated;

/*Generated by MPS */

import pt.isep.enorm.ref.service.ReportOnVideoService;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;
import pt.isep.enorm.ref.domain.ReportOnVideo;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.http.ResponseEntity;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;

public abstract class GeneratedReportOnVideoController {
  private final ReportOnVideoService resourceService;
  protected GeneratedReportOnVideoController(ReportOnVideoService resourceService) {
    this.resourceService = resourceService;
  }
  @GetMapping(value = "reportonvideos")
  public List<ReportOnVideo> listReportOnVideo() {
    return resourceService.listReportOnVideo();
  }
  @GetMapping(value = "reportonvideos/{id}")
  public ReportOnVideo getReportOnVideo(@PathVariable Long id) {
    return resourceService.getReportOnVideo(id);
  }
  @PostMapping(value = "reportonvideos")
  public ResponseEntity<ReportOnVideo> createReportOnVideo(@Valid @RequestBody ReportOnVideo request) {
    return ResponseEntity.status(HttpStatus.CREATED).body(resourceService.createReportOnVideo(request));
  }

  @DeleteMapping(value = "reportonvideos/{id}")
  public ResponseEntity<Void> deleteReportOnVideo(@PathVariable Long id) {
    resourceService.deleteReportOnVideo(id);
    return ResponseEntity.noContent().build();
  }
}
