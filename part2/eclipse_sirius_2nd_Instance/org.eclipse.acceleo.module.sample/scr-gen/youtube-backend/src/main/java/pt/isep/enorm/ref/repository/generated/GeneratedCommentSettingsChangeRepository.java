package pt.isep.enorm.ref.repository.generated;

import pt.isep.enorm.ref.domain.CommentSettingsChange;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface GeneratedCommentSettingsChangeRepository extends JpaRepository<CommentSettingsChange, Long> {
}
