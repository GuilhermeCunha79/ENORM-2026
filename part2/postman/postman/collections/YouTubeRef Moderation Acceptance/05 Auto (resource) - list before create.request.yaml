$kind: http-request
url: "{{baseUrl}}{{resourceListPath}}"
method: GET
headers:
  Authorization: Bearer {{moderatorToken}}
scripts:
  - type: afterResponse
    code: |-
      pm.test("Resource list returns 200", function () {
        pm.response.to.have.status(200);
      });

      const body = pm.response.json();
      pm.test("Resource list response is a collection", function () {
        pm.expect(body).to.be.an("array");
      });

      pm.collectionVariables.set("resourceBeforeCount", String(body.length));
    language: text/javascript
order: 5000
